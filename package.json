{
  "name": "gha-type-guard",
  "private": true,
  "scripts": {
    "build": "ncc build src/index.ts -o dist --minify",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^3.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7",
    "@types/node": "^25.9.1"
  }
}